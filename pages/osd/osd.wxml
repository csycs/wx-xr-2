<view class="container">
  <xr-osd-marker
    disable-scroll
    id="osd-frame"
    width="{{renderWidth}}"
    height="{{renderHeight}}"
    style="width:{{width}}px;height:{{height}}px;"
    marker-src="{{markerSrc}}"
  />
</view>
